# hello123 Local Run Steps

After cloning this repository, run:

```bash
npm init -y
npm install express
node app.js
```

Server runs on:
- API: `http://localhost:3000/weblinks`
- APIDoc: `http://localhost:3000/apidoc`

Use Postman with `http://localhost:3000/weblinks`.
